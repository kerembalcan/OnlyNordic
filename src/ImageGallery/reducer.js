/* @flow */

import {} from "./actionTypes";

export type State = {

}

export const imageGalleryReducerInitialState : State = {

};

export default function imageGalleryReducer(state : State = imageGalleryReducerInitialState, action: Object) {
  switch (action.type) {

    default:
      return state;
  }
}