/* @flow */

import {} from "./actionTypes";

export type State = {

}

export const imageFeedReducerInitialState : State = {

};

export default function imageFeedReducer(state : State = imageFeedReducerInitialState, action: Object) {
  switch (action.type) {

    default:
      return state;
  }
}